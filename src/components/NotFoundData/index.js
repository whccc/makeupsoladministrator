import React from 'react';
import { Container } from './styles.js';
export const NotFoundDataApi = ({ strText }) => {
    return (
        <Container>
            <h3>{strText}</h3>
        </Container>
    );
};
